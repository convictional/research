# Copy this file to .env.secrets and fill in your keys.
# .env.secrets is gitignored (matches .env.* in the monorepo .gitignore).
# The CLI also still reads from .env if present.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
